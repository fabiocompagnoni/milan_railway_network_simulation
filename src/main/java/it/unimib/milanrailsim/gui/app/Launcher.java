package it.unimib.milanrailsim.gui.app;

/** Entry point of the executable jar: a plain class, so the JavaFX launcher check is bypassed. */
public final class Launcher {

	private Launcher() {
	}

	public static void main(String[] args) {
		MilanRailSimApp.main(args);
	}
}
