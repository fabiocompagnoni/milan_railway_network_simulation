<?xml version="1.0" ?>
<!DOCTYPE config SYSTEM "http://www.matsim.org/files/dtd/config_v2.dtd">
<config>

	<module name="railsim">
		<param name="accelerationDefault" value="0.5"/>
		<param name="decelerationDefault" value="0.5"/>
		<param name="pollInterval" value="10.0"/>
		<param name="networkModes" value="rail"/>
		<param name="updateInterval" value="1.0"/>
	</module>

	<module name="global">
		<param name="randomSeed" value="4711"/>
		<param name="coordinateSystem" value="EPSG:32632"/>
	</module>

	<module name="controler">
		<param name="runId" value="milan-baseline"/>
		<param name="outputDirectory" value="output/milan-baseline"/>
		<param name="firstIteration" value="0"/>
		<param name="lastIteration" value="0"/>
	</module>

	<module name="qsim">
		<param name="startTime" value="00:00:00"/>
		<!-- GTFS times run past 24:00 for after-midnight trips -->
		<param name="endTime" value="30:00:00"/>
		<param name="snapshotperiod" value="00:00:00"/>
		<!-- upstream railsim convention: the road engine idles, rail runs in railsim -->
		<param name="mainMode" value="car"/>
		<!-- trains legitimately wait at occupied resources; never teleport them -->
		<param name="stuckTime" value="999999."/>
	</module>

	<module name="transit">
		<param name="routingAlgorithmType" value="SwissRailRaptor"/>
		<param name="transitScheduleFile" value="transitSchedule.xml"/>
		<param name="useTransit" value="true"/>
		<param name="usingTransitInMobsim" value="true"/>
		<param name="vehiclesFile" value="transitVehicles.xml"/>
	</module>

	<module name="network">
		<param name="inputNetworkFile" value="network-with-stations.xml"/>
	</module>

</config>
